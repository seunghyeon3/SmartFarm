<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.smartFarm.shopping.KitMapper">
	<select id="kitSelectList"
		resultType="co.smartFarm.shopping.KitVO">
		SELECT * FROM KIT

	</select>
	
	<update id="kitUpdateWhet" parameterType="co.smartFarm.shopping.KitVO">
		UPDATE KIT
		SET KIT_SALE_WHET = #{kit_sale_whet}
		WHERE KIT_NO = #{kit_no}
	</update>
	
	<select id="kitSelectOne"
		resultType="co.smartFarm.shopping.KitVO"
		parameterType="String">
		SELECT * 
		FROM KIT
		WHERE KIT_NAME LIKE '%' || #{kit_name} || '%'
	</select>

</mapper>