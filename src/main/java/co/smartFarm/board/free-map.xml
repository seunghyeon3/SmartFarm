<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.smartFarm.board.FreeMapper">

    <!-- 전체리스트 -->
    <select id="freeList"
        resultType="co.smartFarm.board.FreeVO">
        SELECT FREE_NO, FREE_TITLE, MEM_NAME, FREE_DAY, FREE_HIT FROM FREE
    </select>

    <!-- 단건조회 -->
    <select id="freeOne"
        resultType="co.smartFarm.board.FreeVO" parameterType="int">
        SELECT * FROM FREE WHERE FREE_NO = #{free_no}
    </select>

</mapper>