<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.smartFarm.board.IntroMapper">
 <!-- 전체리스트 -->
 <select id="introSelectList" resultType="co.smartFarm.board.IntroVO">
 SELECT * FROM INTRO ORDER BY INTRO_NO ASC
 </select>
<!-- 글쓰기 -->
<insert id="introInsert" parameterType="co.smartFarm.board.IntroVO">
<selectKey keyProperty="intro_no" resultType="co.smartFarm.board.IntroVO" order="BEFORE">
SELECT NVL(MAX(INTRO_NO) + 1, 1 ) AS INTRO_NO FROM INTRO
</selectKey>
INSERT INTO INTRO(INTRO_NO,INTRO_IMG,
INTRO_BOARD_CLASS)
VALUES(#{intro_no}, #{intro_img}, #{intro_board_class})
</insert>
<!-- 단건조회 -->
<select id="introSelect" resultType="co.smartFarm.board.IntroVO"
parameterType="int">
SELECT * FROM INTRO WHERE INTRO_NO = #{intro_no}
</select>
<!-- 수정 -->
<update id="introUpdate" parameterType="co.smartFarm.board.IntroVO">
UPDATE INTRO
SET
INTRO_IMG = #{intro_img},
INTRO_BOARD_CLASS = #{intro_board_class}
WHERE INTRO_NO = #{intro_no}
</update>
</mapper>